# cruzAlex Cargo Package List
# Rust-based TUI applications
# Note: Some tools are better installed from system repos (see fedora.txt/arch.txt)

# Shell prompt
starship

# Modern ls replacement
eza

# System monitor (alternative)
bottom

# Network tools
gping
bandwhich

# Process viewer
procs

# HTTP client
xh

# ═══════════════════════════════════════════════════════════════
# The following are commented out - install from repos instead:
# ═══════════════════════════════════════════════════════════════
# yazi-fm yazi-cli   - Install: sudo dnf install yazi (Fedora) or yay -S yazi (Arch AUR)
# lazygit            - Install: sudo dnf copr enable atim/lazygit && sudo dnf install lazygit
# gitui              - Install: sudo dnf install gitui (if available) or build from source
# lazydocker         - Install: go install github.com/jesseduffield/lazydocker@latest
# spotify_player     - Install: needs librespot-devel, build manually
# dust               - Install: sudo dnf install dust (if available)
# satty              - Install: sudo dnf install satty (if available)
