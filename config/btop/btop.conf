# cruzAlex btop Configuration
# TUI system monitor

#* Color theme (set by theme hooks)
color_theme = "/home/cruzalex/.config/btop/themes/cruzalex.theme"

#* Use 24-bit truecolor
truecolor = True

#* Disable gradient effects (cleaner look)
theme_background = False

#* Graph symbol style: "braille", "block", or "tty"
graph_symbol = "braille"

#* Graph symbol for CPU: "default", "braille", "block", "tty"
graph_symbol_cpu = "default"

#* Graph symbol for memory: "default", "braille", "block", "tty"
graph_symbol_mem = "default"

#* Graph symbol for network: "default", "braille", "block", "tty"
graph_symbol_net = "default"

#* Graph symbol for processes: "default", "braille", "block", "tty"
graph_symbol_proc = "default"

#* Shown boxes: "cpu mem net proc"
shown_boxes = "cpu mem net proc"

#* Update time in milliseconds
update_ms = 1000

#* Processes sorting: "pid", "program", "arguments", "threads", "user", "memory", "cpu lazy", "cpu direct"
proc_sorting = "cpu lazy"

#* Reverse sorting
proc_reversed = False

#* Show processes as tree
proc_tree = False

#* Process tree auto collapse on low memory
proc_tree_auto_collapse = 2000

#* Check for updates on startup
update_check = False

#* Show detailed CPU info in task manager
proc_info_smaps = False

#* Show process CPU usage in threads
proc_per_core = False

#* Show CPU graph per core
cpu_graph_upper = "total"
cpu_graph_lower = "total"

#* Show CPU temps
show_cpu_freq = True
show_coretemp = True

#* CPU temp sensor location
cpu_sensor = "Auto"

#* Show GPU stats (if available)
show_gpu_info = "Auto"

#* Memory display: "total", "used", "free", "cached", "available"
mem_graphs = True

#* Network interface to show
net_iface = ""

#* Network download/upload text position: "L" left, "R" right
net_download = "L"
net_upload = "R"

#* Sync network graph scales
net_sync = True

#* Network interface auto mode
net_auto = True

#* Show battery status
show_battery = True

#* Clock format
clock_format = "%X"

#* Show init info on startup
show_init = False

#* Logging level: "ERROR" "WARNING" "INFO" "DEBUG"
log_level = "WARNING"

#* Vi mode (hjkl navigation)
vim_keys = True

#* Rounded corners (supported in terminals with box drawing)
rounded_corners = True

#* Show per-core/thread CPU load
cpu_single_graph = False

#* Show swap disk
swap_disk = True

#* Show disk I/O activity
show_io_stat = True

#* Show disks with only specific mount points
disks_filter = ""

#* Enable filter for processes
proc_filter_kernel = False

#* Enable mouse support
proc_gradient = True

#* Enable selection with keyboard
enable_gou = True
